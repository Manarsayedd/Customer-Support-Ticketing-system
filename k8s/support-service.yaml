apiVersion: apps/v1
kind: Deployment
metadata:
  name: support-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: support-service
  template:
    metadata:
      labels:
        app: support-service
    spec:
      containers:
      - name: support-service
        image: support-service:latest
        imagePullPolicy: Never
        env:
        - name: DB_HOST
          value: "postgres"
        - name: DB_USER
          value: "k8s_user"
        - name: DB_PASSWORD
          value: "k8s_password"
        - name: DB_NAME
          value: "ticketing"
        ports:
        - containerPort: 3002
---
apiVersion: v1
kind: Service
metadata:
  name: support-service
spec:
  type: ClusterIP
  selector:
    app: support-service
  ports:
  - port: 3002
    targetPort: 3002
